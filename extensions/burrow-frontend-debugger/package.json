{
	"name": "burrow-frontend-debugger",
	"displayName": "Burrow Frontend Debugger",
	"description": "Visual React component debugger as an editor panel: embeds the target app via the tools/frontend-debugger sidecar and bridges component/CSS reveals into the editor. See docs/architecture/15-frontend-debugger.md.",
	"version": "0.1.0",
	"publisher": "burrow",
	"license": "MIT",
	"engines": {
		"vscode": "^1.128.0"
	},
	"categories": [
		"Other"
	],
	"activationEvents": [],
	"main": "./out/extension.js",
	"capabilities": {
		"virtualWorkspaces": false,
		"untrustedWorkspaces": {
			"supported": false
		}
	},
	"scripts": {
		"compile": "gulp compile-extension:burrow-frontend-debugger",
		"watch": "gulp watch-extension:burrow-frontend-debugger"
	},
	"contributes": {
		"commands": [
			{
				"command": "burrow.frontendDebugger.open",
				"title": "Open Frontend Debugger",
				"category": "Burrow"
			},
				{
					"command": "burrow.frontendDebugger.openInBrowser",
					"title": "Open App in Browser (Inspector)",
					"category": "Burrow"
				},
			{
				"command": "burrow.frontendDebugger.isolate",
				"title": "Isolate Component",
				"category": "Burrow",
				"icon": "$(open-preview)"
			},
				{
					"command": "burrow.frontendDebugger.reloadPreview",
					"title": "Reload Preview",
					"category": "Burrow",
					"icon": "$(refresh)"
				},
			{
				"command": "burrow.frontendDebugger.pickSample",
				"title": "Pick Sample Props",
				"category": "Burrow",
				"icon": "$(symbol-property)"
			},
			{
				"command": "burrow.frontendDebugger.toggleMode",
				"title": "Frontend Debugger: Toggle Mock/Live",
				"category": "Burrow"
			},
			{
				"command": "burrow.frontendDebugger.restart",
				"title": "Frontend Debugger: Restart Sidecar",
				"category": "Burrow"
			},
			{
				"command": "burrow.frontendDebugger.stop",
				"title": "Frontend Debugger: Stop Sidecar",
				"category": "Burrow"
			},
			{
				"command": "burrow.frontendDebugger.showLogs",
				"title": "Frontend Debugger: Show Logs",
				"category": "Burrow"
			}
		],
		"menus": {
			"editor/title": [
				{
					"command": "burrow.frontendDebugger.isolate",
					"when": "resourceExtname == .tsx || resourceExtname == .jsx",
					"group": "navigation@9"
				},
					{
						"command": "burrow.frontendDebugger.reloadPreview",
						"when": "activeWebviewPanelId == burrow.frontendIsolation",
						"group": "navigation@1"
					},
					{
						"command": "burrow.frontendDebugger.pickSample",
						"when": "activeWebviewPanelId == burrow.frontendIsolation",
						"group": "navigation@2"
					}
			]
		},
		"configuration": {
			"title": "Frontend Debugger",
			"properties": {
				"burrow.frontendDebugger.targetDir": {
					"type": "string",
					"default": "",
					"description": "Absolute path to the target Vite frontend. Empty = auto-detect (<workspace>/nodewatch/frontend if it exists, else the workspace folder)."
				},
				"burrow.frontendDebugger.repoRoot": {
					"type": "string",
					"default": "",
					"description": "Target repo root (so @shared resolves). Empty = first workspace folder."
				},
				"burrow.frontendDebugger.backendTarget": {
					"type": "string",
					"default": "http://localhost:8080",
					"description": "Live-mode proxy target (the Go backend under debug)."
				},
				"burrow.frontendDebugger.mode": {
					"type": "string",
					"enum": [
						"mock",
						"live"
					],
					"default": "mock",
					"description": "Data mode at sidecar boot. Runtime flips via the status bar are ephemeral."
				},
				"burrow.frontendDebugger.uiPort": {
					"type": "number",
					"default": 6080,
					"description": "Debugger UI/API port. Falls back to a free port if taken."
				},
				"burrow.frontendDebugger.targetPort": {
					"type": "number",
					"default": 5180,
					"description": "Embedded target dev-server port. Falls back to a free port if taken."
				},
				"burrow.frontendDebugger.targetBase": {
					"type": "string",
					"default": "/watch/app/",
					"description": "Base path the target app is served under."
				},
				"burrow.frontendDebugger.toolPath": {
					"type": "string",
					"default": "",
					"description": "Override the sidecar location. Empty = <repo>/tools/frontend-debugger."
				},
				"burrow.frontendDebugger.runCommand": {
					"type": "string",
					"default": "",
					"description": "Command that starts YOUR dev server, run verbatim in a terminal for \"Open App in Browser\" (e.g. npm run dev). Empty = use the built-in instrumented sidecar (overlay pre-injected, zero setup). For the verbatim path, add the Burrow inspector Vite plugin to your config so \u2325-click reveal works."
				},
				"burrow.frontendDebugger.appUrl": {
					"type": "string",
					"default": "",
					"description": "URL to open in the browser. Empty = auto (the runCommand dev-server URL, or the sidecar target URL)."
				}
			}
		}
	}
}
