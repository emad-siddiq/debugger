# WO-00 report — baseline commits + three-track recon
STATUS: DONE

## Changed
- `burrow/` (nested repo, branch `main`, **not pushed**): **10 commits `69a5b16..4d73058`**, tag **`baseline/pre-integration-run`** on HEAD. task-01+task-02 were entirely uncommitted; now bisectable:
  `feat`(fork/branding/burrow-core) · `chore`(strip inventory) · `build`(patch0001 gulpfile) · 5× `strip:` dir-deletions (**64 dirs / 5240 files**: non-Go 39, notebook+web 5, JS-runners 4, accounts/AI 7, themes 9) · `build`(patch0002 unwire) · `strip`(patch0003 leaf contribs).
- Deviations from STRIP.md's 8-commit plan (**final tree byte-identical — verified**): (1) STRIP#7 `product.json drop js-debug` folded into the task-01 commit — its js-debug/auth hunks interleave with identity hunks in the same regions, not cleanly splittable; (2) patch-0001 initial-form (burrow-core registration) rides in the gulpfile commit, not a separate task-01 commit (one file, one patch); (3) added `chore: strip inventory` and `strip: leaf contributions (patch 0003)` — both present in the tree, absent from STRIP.md's 8.
- Part 2 = strictly read-only; no source changes.

## Verified
- compile: `npm run compile` → **0 errors** (compile-src + client, 31s).
- boot: clean **10-process** tree from `/tmp/bw` (scrubbed VSCODE_*/ELECTRON_*), extension host up, `burrow-core` activated, **zero crash markers pre-teardown**.
- baseline integrity: `HEAD^{tree}` == pre-commit `git write-tree` (**byte-identical**); working tree clean; still on `main`, no branch created/switched.

## Discoveries
- **burrow-core `configurationDefaults` silently drops 2 keys** at boot: `terminal.integrated.inheritEnv` and `workbench.welcomePage.experimentalOnboarding` ("Cannot register… only machine-overridable/window/resource/language scopes"). Those task-02 baseline defaults never take effect — latent bug.
- **Retained load-bearing `defaultChatAgent` → AgentHost probes api.github.com ~20× at boot** ("No token resolved for resource: https://api.github.com/repos"). Brushes the "zero non-user-initiated network" invariant until chat-excision.
- **Stack**: the only hard break from the backend deletion is the compose `ide` service (`docker-compose.yml:62` → deleted `backend/Dockerfile`); `db`/`pgweb`/`emitter-*` are now orphaned. Frontend **mock mode is independent**; live mode (`NW_BACKEND_TARGET=ide:8080`) is dead.

## Decisions
- made — **RD = layer-3 core patch (WO-1), NOT config.** Extensions can't relocate a built-in container (`viewsExtensionPoint.ts:64` allows only activitybar/panel; `views.customizations` is fragile PROFILE storage). Patch ≈**4-6 lines / 3 files**: `debug.contribution.ts:466` `Sidebar→AuxiliaryBar`; `debugService.ts:647` + `debugSession.ts:1393` swap hardcoded-`Sidebar` `openPaneComposite` → location-resolving `viewsService.openViewContainer` (auto-reveals the hidden aux bar via `paneCompositePart.ts:523`; `debugSession` needs `IViewsService` injected). ⌥⌘D is **free** (⇧⌘D stays debug-open) → bind `workbench.action.toggleAuxiliaryBar` in the extension package.json (layer 4). Optional `layout.ts:2855` default flip (not needed). New ledger 0004.
- made — **IX inspector = real workbench `ViewPane`, NOT webview.** Native `WorkbenchList`/`WorkbenchAsyncDataTree` give virtualization (the 50k-slice requirement), keyboard nav, cross-stop identity/expansion (`identityProvider=getId`), theming, and a lazy async-data-source 1:1 with DAP paging — all free; a webview reimplements each + a postMessage bridge to reach the model (`webviewViewPane.ts:130-234` shows the burden). Miller columns = N side-by-side lists in one ViewPane; the value-pane visualizer slot (task 06) is the only webview. Model surface: `IExpressionContainer.getChildren` / `session.variables(ref,tid,filter,start,count)` (paging, `BASE_CHUNK_SIZE=100`), `ExpressionContainer.allValues`+`valueChanged` (diff, one-shot reset at paint), `onDidFocusStackFrame`/`onWillUpdateViews`. Register into the debug container; re-gate stock `VariablesView` behind an inverted `CONTEXT_DEBUG_UX`. This is the plan's pre-approved big patch — ledger honestly.
- made — **WO-2 Delve = hand-rolled minimal `dlv dap` adapter, DEFER vscode-go vendoring.** dlv is itself a DAP server; a layer-4 `burrow-go-debug` extension with a `DebugAdapterExecutable`(`dlv dap`) + `go` debug type + a fixture launch config yields a live stopped session in one extension, **no core patch** (debug core/model are intact). All of task 03 (go-base vendor, gopls, scheme bar, tool manager) defers — IX builds on the DAP model, not task 03. Use host `dlv` for the fixture; pinning is a task-03 concern.
- made — **FD = A (Embed + bridge), NOT B (CDP-native).** The existing tool is a portable Vite/React SPA already iframed (`TargetPane.tsx:124`); box-model + CSS data are CSSOM + `getComputedStyle` posted from injected `agent.js` (**no CDP**). A = host the existing `frontend/server` sidecar (target-Vite + agent injection + `/api` file ops) in a Burrow webview by **full public origin** (never `asExternalUri`) + a bridge command `reveal rule → showTextDocument`, reusing `/api/css/locate`. B = rewrite every panel against CDP + Electron `webContents.debugger` plumbing, discarding the React-fiber-aware inspection and JSX `data-inspect-*` source-stamping that CDP cannot see. The authored-CSS gap (today **line-only, single-file `src/index.css`, zero source-map support**) is a shared data-layer problem, cheaper to fix inside A (teach `agent.js`/sidecar `styleSheets[].href` + CSS source maps). FD → task 15; write `docs/architecture/15-frontend-debugger.md` once fixed.
- needed — none blocking. For re-plan: the two Discoveries (config-defaults bug; AgentHost-at-boot vs the network invariant) — pull chat-excision forward, or accept until its task? Recommend: cheap fix for the config-defaults keys now; keep chat-excision as its own task but confirm AgentHost is inert without a token.

## Next
- **WO-1 (RD):** apply the 3-file aux-bar patch (ledger 0004) + ⌥⌘D keybinding; verify a fresh profile boots Run&Debug in the right aux bar, auto-reveals on session start, toggles. No adapter needed — verifiable with a launch that errors only at adapter-resolve.
- **WO-2 (IX prereq):** `burrow-go-debug` minimal `dlv dap` adapter + `testdata/debuggee` fixture → live stopped session feeding the stock Variables tree (proves the DAP model for WO-3+).
- Risks: mixed product.json hunks make future per-task product.json archaeology impossible (immaterial — tree correct, verified); FD live-mode stays dead until a backend target returns (not needed to dock FD).
